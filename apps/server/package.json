{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@blockade/env": "workspace:*",
    "@blockade/api": "workspace:*",
    "@blockade/auth": "workspace:*",
    "@blockade/db": "workspace:*",
    "hono": "catalog:",
    "@hono/node-server": "^2.0.8",
    "@trpc/server": "catalog:",
    "@hono/trpc-server": "^0.4.2",
    "better-auth": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsdown": "^0.22.3",
    "@blockade/config": "workspace:*",
    "tsx": "^4.22.5",
    "@types/node": "catalog:"
  }
}